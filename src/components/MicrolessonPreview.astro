---
const lessons = await Astro.glob('../content/microlessons/*.md');
const latest = lessons.sort((a, b) => b.frontmatter.date.localeCompare(a.frontmatter.date))[0];
---
<section class="my-8">
  <h2 class="text-2xl font-semibold text-blue-300">Today's Microlesson</h2>
  <h3 class="text-lg mt-2 text-white">{latest.frontmatter.title}</h3>
  <p class="text-gray-300 mt-1">{latest.frontmatter.summary}</p>
  <a href="/daily" class="text-blue-400 underline mt-2 inline-block">Read More</a>
</section>
